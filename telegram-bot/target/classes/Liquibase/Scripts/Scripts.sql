-- liquibase formatted sql

-- changeset dastrashenya:1
CREATE table notification_task
    (
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    chat_id    BIGINT,
    notification     TEXT NOT NULL,
    date_time TIMESTAMP NOT NULL)
;